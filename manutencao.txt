erros de portugues quebrado usar:ISO 8859-1

💥executar sem usar o botao já que parou de rodar no meu not antido, executar via terminal:
🔹gcc main.c -o  main.exe 
            ou 
gcc main.c -o  output/main.exe
🔹./main.exe 
    ou 
./output/main.exe

acho que tem erros:
*tudo salva os arquivos.txt a pasta output mmanualmente para salvar reabrir.
Exemplo ⬇️ 
✅ "output/atividades.txt", //fazer umja classe para testar isso na hora de abrir o arquivo.

vai refazer:
7 salvar num arquivo

ja resolvidos:
    1. bugs de buffer no 4. provas e no 8. sortear atividades ✅
    2. conferir se todos os menus se possui ⬇️ ✅
                    printf("%s\n", blue("Saindo do programa..."));
                    sleep(1);  // pausa de 2 segundos
    3. corrigindo a chanse de bug no apagar.h

✅ Ideias pra fazer ainda
☣Quando for implementar IA impedir que o usuario crie muitas atividades, fazer um algoritmo que so permita denovo depois de 1miin ou 2 min.